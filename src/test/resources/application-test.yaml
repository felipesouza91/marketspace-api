spring:
  profile:
    profiles:
      active: test,minio
  application:
    name: marketspace-api
  liquibase:
    change-log: classpath:/db/db.changelog-master.yaml
  jpa:
    show-sql: true

  servlet:
    multipart:
      max-file-size: 2MB
      max-request-size: 10MB

api:
  security:
    token:
      secret: ${TOKEN_SECRET:ZTNiMGM0NDI5OGZjMWMxNDlhZmJmNGM4OTk2ZmI5MjQyN2FlNDFlNDY0OWI5MzRjYTQ5NTk5MWI3ODUyYjg1NQ==}
      expiration-time: ${TOKEN_EXPIRATION_TIME:60}
  storage:
    minio:
      bucketName: ${BUCKET_NAME:marketstore}
      url: ${MINIO_URL:http://localhost:9000}
      accessKey: ${MINIO_ACCESS_KEY:Zw0npb3JKMOCTF2gmkqW}
      accessSecret: ${MINIO_ACCESS_SECRET:S5Kek7cQkIl7Vv31EPWzYIo3JIz9OQ1gIBGPblUu}